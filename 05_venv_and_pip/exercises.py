"""
These are terminal/workflow exercises, not code-completion — do them in
your shell, then optionally note the commands you ran as comments here.
"""

# 1. Create a venv in this folder called `.venv`, activate it, and run
#    `python3 example.py`. Confirm it prints "Running inside a venv: True".
#    Deactivate and run it again — confirm it now says False.


# 2. With the venv activated, `pip install requests`. Run `pip freeze` and
#    save the output to a `requirements.txt` file in this folder. Then
#    `pip uninstall requests` and reinstall everything from the file with
#    `pip install -r requirements.txt` to prove the file works.


# 3. Add `.venv/` to a `.gitignore` file in this folder (create it if it
#    doesn't exist). Explain to yourself in a one-line comment why the venv
#    itself should never be committed but requirements.txt should be.
